exercice 1 :
Algorithm valeur_absolue 
   var: a réel 
début
  écrire("entrer un réel a")
  lire(a)
  si (a>=0) alors
     érire("valeur absolue de a = :",a)
  sinon 
    écrire("valeur absolue de a =:", -a)
  fin si 
fin

exercice 2 :
Algorithm pair_ou_impair
   var : a réel 
début  
   écrire("entrer un réel a")
   lire(a)
   si (a mod 2) == 0 alors
      écrire ("ce nombre est pair")
	sinon 
	  écrire ("ce nombre est impaire")
	fin si
fin

exercice 3:
Algorithm Alphabetic
   var: l1,l2,l3 caractéres
début 
   écrire("entrer le 1 ér caractére")
   lire(l1)
   écrire("entrer le 2éme caractére")
   lire(l2)
   écrire("entrer le 3éme caractére")
   lire(l3)

   si (ord(l2) == ord(l1)+1) et (ord(l3) == ord(l2)+1) alors
       écrire("les letteres sont éntrés alphabetiquement rangés")
   sinon 
	   écrire("les letteres sont éntrés alphabetiquement rangés")
	fin si
fin

exercice 4 :
Algorithm maximum
   var : x,n,a réels
début 
    écrire("entrer un nombre: ")
	lire(a)
	max<---a
	écrire(a)
	
	écrire("enter un nombre")
	lire(n)
	
	pour i de 0 a n-1 faire
	    écrire("entrer un nombre: ")
	    lire(x)
		écrire(x)
		si(x>max) alors 
		  max<--x
		  écrire("le max est " , max)
        fin si
    fin pour
fin	
 
exercice 5 : 
Algorithm produit
    var : a,b réels
début
   écrire("entrer un nombre a")
   lire(a)
   écrire("entrer un nombre b")
   lire(b
   
   si(a>0 et b>0) ou (a<0 et b<0) alors
     écrire(" le produit de a et b est positif")
   sinon 
     écrire(" le produit est négatif")
   fin si
fin

exerice 6 :
Algorithm jour
    var : jour entiers
début
  écrire("donner un entier entre 1 et 7")
  lire(jour)
  
  si (jour == 1) alors 
     écrire("le jour est dimanche")
  sinon si (jour == 2) alors
     écrire("le jour est lundi")
  sinon si (jour == 3)
     écrire("le jour est mardi")
  sinon si (jour == 4)
     écrire("le jour est mercredi")
  sinon si (jour == 5)
     écrire("le jour est jeudi")
  sinon si (jour == 6)
     écrire("le jour est vendredi")
  sinon
     écrire("le jour est samedi")
  fin si
fin



     
  	 

   
Algorithm 